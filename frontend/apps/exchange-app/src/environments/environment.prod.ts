export const environment = {
  production: true,
  apiUrl: '/api/v1',
  wsUrl: '/ws',
  
  // Feature flags
  features: {
    darkMode: true,
    advancedTrading: true,
    marginTrading: false,
    futures: false
  }
};
